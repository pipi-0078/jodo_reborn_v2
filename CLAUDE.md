# 浄土再現プロジェクト 作業規約

## トークン節約(cost_optimization クックブックの適用)

コンテキストに入るものだけが費用。以下を守る。

### 画像 — 最大の節約点
- **私が検品する画像は必ず縮小してから見る**: `tools/inspect.sh <png>` で640px幅に落とす
  (1280x800=約1,365トークン → 640x400=約341トークン、75%減)
- **施主へ送る画像(SendUserFile)は縮小しない**。私のコンテキストを通らないので無料
- 撮影は必要な枚数だけ。同じ角度を撮り直さない

### 巨大なツール出力を避ける
- GitHub ActionsのMCP `actions_list` は1回で7万〜29万文字を返す。**呼ばない**
  デプロイ確認は `git log --oneline -1` とプッシュ成功をもって足れりとし、
  どうしても必要なときだけ呼んでファイルをgrepする
- ファイルは全読みせず `sed -n '10,40p'` / `grep -n` で必要箇所だけ
- 一度読んだファイルを再読しない(編集は Edit/sed で差分のみ)

### 検品は目視より数値
- アセットの接合確認は、スクショを何枚も見るのではなく
  Blender内でワールド座標を出力して数値で判定する(橋の一件の教訓)
- 数値で当たりを付けてから、確認のスクショは1〜2枚に留める

### 自分の出力
- 報告は要点のみ。作業ログの逐一実況をしない
- コード編集は差分最小。ファイル全書き換えを避ける

## プロジェクト構成
- `src/` メイン空間(main.ts)とギャラリー(gallery.ts)
- `tools/make_*.py` Blenderによるアセット生成(bpy、ヘッドレス)
- `public/assets/` 完成アセット + gallery.json(陳列台帳)
- `diary/` X投稿用の作業記録(Obsidianへ自動同期)
- `.claude/skills/x-worklog/` 日記作成スキル

## 環境復元(コンテナは使い捨て)
    pip install bpy pillow numpy
    npm ci
